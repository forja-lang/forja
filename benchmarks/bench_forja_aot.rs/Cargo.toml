[package]
name = "bench_forja_aot"
version = "0.7.0"
edition = "2021"

# Exportado por Forja (fa) desde bench_forja_aot.fa (podés ejecutar directo con 'forja ejecutar')
# https://github.com/lococoi/forja

[dependencies]
