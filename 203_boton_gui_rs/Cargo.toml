[package]
name = "forja_203_boton_gui"
version = "0.3.0"
edition = "2021"

# Exportado por Forja (fa) desde 203_boton_gui.fa (podés ejecutar directo con 'forja ejecutar')
# https://github.com/lococoi/forja

[dependencies]
# GUI nativa con Xilem (framework UI reactivo con GPU)
xilem = "0.4"
