[package]
name = "forja_204_login_final"
version = "0.7.0"
edition = "2021"

# Exportado por Forja (fa) desde 204_login_final.fa (podés ejecutar directo con 'forja ejecutar')
# https://github.com/lococoi/forja

[dependencies]
# GUI nativa con Xilem (framework UI reactivo con GPU)
xilem = "0.4"
